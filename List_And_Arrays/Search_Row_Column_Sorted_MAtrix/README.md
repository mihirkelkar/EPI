Given a matrix that is sorted row wise and column wise, search for a given number


Start with the right most element
  if element e > x
  then move down
  if element e == x: return 
  else move left
